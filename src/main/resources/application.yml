spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/ramenRecord
    username: iidahigashi
    password: postgres
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB